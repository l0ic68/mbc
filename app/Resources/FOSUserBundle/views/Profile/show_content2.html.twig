{% trans_default_domain 'FOSUserBundle' %}

<div class="section no-pad-bot " id="index-banner" style="padding-top:80px" >
    <div class="  full-width z-depth-2">
        <div class="row center">
            <div class="col m12 back-img white">
                <div>
                    <h4 class="gray marg-top bold">Vos informations de profil</h4>
                    <div class="col m12">
<div class="fos_user_user_show">
    <p>Nom de l'entreprise : {{ user.nomEntreprise }}</p>
    <p>Adresse e-mail : {{ user.email }}</p>
    <p>Adresse : {{ user.adresse }}</p>
    <p>Code postal : {{ user.codePostal }}</p>
    <p>Nom interlocuteur : {{ user.nomInterlocuteur }}</p>
    <p>Position interlocuteur : {{ user.positionInterlocuteur }}</p>
    <a href ="{{ path('fos_user_profile_edit') }}" class="waves-effect waves-light btn"><i class="material-icons left">edit</i>Modifier vos informations</a>

    <p>PAGE 2</p>
    <a href="{{ path('retours', {'id': app.user.id }) }}" class="btn">regardez les retours de vos annonces (chiffre total de retours : x)</a>
    <p>PAGE 2</p>

    
</div>
<h4 class="gray marg-top bold">Vos annonces</h4>
    <table class="striped">
            <thead>
            <tr>
                <th>Name</th>
                <th>description</th>
                <th>nombre de vues</th>
                <th>Annonce postée le</th>
            </tr>
            </thead>

            <tbody>
        {% for offre in offres %}
          <tr>
            <td><a href="{{ path('offre', {'id': offre.id }) }}">{{ offre.name }}</a></td>
            <td>{{offre.description | slice(0, 30)}}</td>
            <td>{{offre.price}}</td>
            <td>{{offre.dateOffre|date("d/m/Y", "Europe/Paris")}}</td>
            <td><a href="{{ path('edit_offre',{id :  offre.id}) }}" class="waves-effect waves-light btn" style="
    width: 77px"><i class="material-icons left">edit</i></a></td>
            <td><a href="{{ path('delete_offre',{id :  offre.id}) }}" class="waves-effect waves-light btn" style="width: 77px"><i class="material-icons left">delete</i></a></td>
          </tr>
        {% endfor %}
        <td><a href="{{ path('add_offre')}}" class="waves-effect waves-light btn" style="width: 77px"><i class="material-icons">note_add</i></a>
                </tbody>
      </table>
</div>
</div>
</div>
</div>
</div>
</div>
